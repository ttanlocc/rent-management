---
phase: 05-lease-management
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - components/leases/LeaseCard.tsx
  - components/leases/LeaseList.tsx
  - components/leases/LeaseForm.tsx
  - components/leases/LeaseEmptyState.tsx
  - components/leases/LeaseScheduleTable.tsx
  - app/(dashboard)/dashboard/leases/page.tsx
  - components/layout/Sidebar.tsx
  - components/layout/BottomNav.tsx
autonomous: true

must_haves:
  truths:
    - "Landlord can create lease with start/end dates, monthly rent, deposit, linked to tenant and room"
    - "Landlord can view lease list with status indicators (active, expiring soon, expired, terminated)"
    - "Landlord can view payment schedule table showing expected vs actual payments per month"
    - "Landlord can edit lease terms and see schedule update"
    - "Landlord can terminate lease early from the UI"
    - "Navigation includes lease management link in sidebar and bottom nav"
  artifacts:
    - path: "components/leases/LeaseCard.tsx"
      provides: "Individual lease display with status badge and key details"
      min_lines: 40
    - path: "components/leases/LeaseList.tsx"
      provides: "Lease list with loading/empty states"
      min_lines: 20
    - path: "components/leases/LeaseForm.tsx"
      provides: "Create/edit form for lease with all fields"
      min_lines: 80
    - path: "components/leases/LeaseEmptyState.tsx"
      provides: "Empty state when no leases exist"
      min_lines: 15
    - path: "components/leases/LeaseScheduleTable.tsx"
      provides: "Payment schedule table showing monthly dues and status"
      min_lines: 40
    - path: "app/(dashboard)/dashboard/leases/page.tsx"
      provides: "Leases management page with CRUD and filtering"
      min_lines: 80
  key_links:
    - from: "app/(dashboard)/dashboard/leases/page.tsx"
      to: "hooks/useLeases.ts"
      via: "useLeases, useCreateLease, useUpdateLease, useDeleteLease, useTerminateLease"
      pattern: "useLeases|useCreateLease"
    - from: "components/leases/LeaseForm.tsx"
      to: "lib/validations/lease.ts"
      via: "leaseFormSchema for form validation"
      pattern: "leaseFormSchema"
    - from: "components/layout/Sidebar.tsx"
      to: "app/(dashboard)/dashboard/leases/page.tsx"
      via: "navigation link"
      pattern: "/dashboard/leases"
---

<objective>
Build the complete lease management UI: form for creating/editing leases, list with status indicators, payment schedule table, lease page with filtering, and navigation integration.

Purpose: Delivers the user-facing lease management experience. Landlords can create leases linked to tenants/rooms, view auto-generated payment schedules, edit terms, and terminate leases early. Status badges (active/expiring/expired/terminated) provide at-a-glance visibility.
Output: Lease components, page, and navigation updates.
</objective>

<execution_context>
@C:/Users/LocTran/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LocTran/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-lease-management/05-01-SUMMARY.md
@.planning/phases/05-lease-management/05-02-SUMMARY.md

Reference files for patterns:
@components/payments/PaymentCard.tsx (card component pattern)
@components/payments/PaymentList.tsx (list component pattern)
@components/payments/PaymentForm.tsx (form component pattern)
@components/payments/PaymentEmptyState.tsx (empty state pattern)
@app/(dashboard)/dashboard/payments/page.tsx (page pattern with CRUD and filtering)
@components/layout/Sidebar.tsx (navigation update)
@components/layout/BottomNav.tsx (navigation update)
@lib/utils/formatters.ts (formatCurrency, formatDate)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lease UI components</name>
  <files>components/leases/LeaseCard.tsx, components/leases/LeaseList.tsx, components/leases/LeaseForm.tsx, components/leases/LeaseEmptyState.tsx, components/leases/LeaseScheduleTable.tsx</files>
  <action>
Create five lease components following the exact patterns from components/payments/*.

**LeaseEmptyState.tsx:**
- Centered empty state matching PaymentEmptyState pattern
- FileText icon (from lucide-react)
- Vietnamese heading: "Chưa có hợp đồng nào"
- Subtext: "Tạo hợp đồng thuê để theo dõi thanh toán tự động"
- "Tạo hợp đồng" button with Plus icon
- Props: onAdd callback

**LeaseCard.tsx:**
- Display lease with key details: tenant name, room name, monthly rent, deposit, start/end dates, status
- Status badge with color mapping:
  - active → Badge variant="default" with green styling (bg-green-100 text-green-800): "Đang hoạt động"
  - expiring_soon → Badge variant="secondary" with amber styling (bg-amber-100 text-amber-800): "Sắp hết hạn"
  - expired → Badge variant="outline" with gray styling: "Đã hết hạn"
  - terminated → Badge variant="destructive": "Đã chấm dứt"
- Expiring soon detection: lease end_date is within 30 days from now AND status is 'active'
  - If detected and status shows 'active', display the amber "Sắp hết hạn" badge instead
- Show formatted dates using formatDate (DD/MM/YYYY)
- Show formatted currency for monthly_rent and deposit using formatCurrency
- Action buttons: "Xem chi tiết" (eye icon), "Sửa" (edit icon), "Chấm dứt" (ban icon, only if active/expiring_soon), "Xóa" (trash icon)
- Props: lease (LeaseWithTenant type), onView, onEdit, onTerminate, onDelete callbacks
- Use Card, CardContent from shadcn/ui

**LeaseList.tsx:**
- Loading state: 3 Skeleton cards
- Empty state: renders LeaseEmptyState
- Maps leases to LeaseCard components in space-y-3 layout
- Props: leases, isLoading, onAdd, onView, onEdit, onTerminate, onDelete

**LeaseForm.tsx:**
- React Hook Form with zodResolver using leaseFormSchema
- Form header: FileText icon + "Tạo hợp đồng" / "Sửa hợp đồng" heading
- Fields (grid gap-4 sm:grid-cols-2):
  1. Tenant selection (sm:col-span-2): Select dropdown with tenant list showing "name - room name"
  2. Room selection (sm:col-span-2): Select dropdown with room list showing "room name (floor X)"
  3. Start date: Input type="date"
  4. End date: Input type="date"
  5. Monthly rent: Input type="number" step="1000" with Banknote icon
  6. Deposit: Input type="number" step="1000" with Banknote icon
  7. Notes (sm:col-span-2): Textarea with placeholder "Ghi chú thêm về hợp đồng..."
- When tenant is selected, auto-populate room_id from tenant's current room (if assigned)
- When room is selected, auto-populate monthly_rent from room's base_rent
- In edit mode: populate form with existing lease data, convert ISO date strings to Date objects
- Footer: "Hủy" (cancel) + "Tạo hợp đồng" / "Lưu thay đổi" (submit) buttons
- Props: lease? (for edit mode), tenants, rooms, onSubmit, onCancel, isSubmitting
- Vietnamese labels throughout with proper diacritics

**LeaseScheduleTable.tsx:**
- Table displaying payment schedule for a lease
- Columns: "Tháng" (month/year from due_date), "Ngày đến hạn" (due_date formatted), "Số tiền" (expected_amount formatted), "Trạng thái" (status badge), "Thanh toán" (link to payment if payment_id exists)
- Status badge colors:
  - pending → default/outline: "Chờ thanh toán"
  - paid → green: "Đã thanh toán"
  - overdue → destructive/red: "Quá hạn"
  - partial → secondary/amber: "Thanh toán 1 phần"
- Overdue auto-detection: if status is 'pending' AND due_date < today, show as "Quá hạn" with red styling
- If payment_id exists, show link text "Xem" that could navigate to payment
- Show summary row at bottom: total expected, total paid, total remaining
- Props: schedules (LeaseSchedule[]), lease (for context)
- Use Table, TableHeader, TableRow, TableHead, TableBody, TableCell from shadcn/ui (create if not exists, or use div-based table)
  </action>
  <verify>
All 5 component files exist in components/leases/. Run `npx tsc --noEmit` to verify TypeScript compilation. Check that all components import from correct paths (@/lib/validations/lease, @/lib/types/database, @/components/ui/*, @/hooks/useLeases).
  </verify>
  <done>
Five lease components created: LeaseEmptyState (empty UI), LeaseCard (individual lease with status badges), LeaseList (list with loading/empty states), LeaseForm (create/edit with auto-population), LeaseScheduleTable (payment schedule display with overdue detection).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create leases page and update navigation</name>
  <files>app/(dashboard)/dashboard/leases/page.tsx, components/layout/Sidebar.tsx, components/layout/BottomNav.tsx</files>
  <action>
**app/(dashboard)/dashboard/leases/page.tsx:**

Follow the exact pattern from app/(dashboard)/dashboard/payments/page.tsx.

Page state:
- dialogMode: 'closed' | 'create' | 'edit' | 'detail'
- selectedLease: lease object or null
- statusFilter: string (lease status value)
- tenantFilter: string (tenant_id value)

Data fetching:
- useProperties() for property context
- useTenants({}) for tenant list (form dropdown + filter)
- useRooms({}) for room list (form dropdown) — import useRooms from hooks/useRooms
- useLeases({ status, tenant_id }) with filters
- useLease(selectedLease?.id) for detail view with schedules

Mutations: useCreateLease, useUpdateLease, useDeleteLease, useTerminateLease

Page layout:
- Header: "Quản lý hợp đồng" heading + count subtitle + "Tạo hợp đồng" button (FileText + Plus icons)
- No property warning: amber Card with AlertTriangle (same as payments page)
- Filter bar (flex flex-wrap gap-3):
  - Status Select: "Tất cả trạng thái" + LEASE_STATUSES options
  - Tenant Select: "Tất cả khách thuê" + tenant list
- LeaseList component with callbacks
- Create/Edit modal: fixed overlay with Card containing LeaseForm
- Detail modal: fixed overlay with Card containing:
  - Lease details header (tenant name, room, dates, rent, deposit, status)
  - LeaseScheduleTable showing payment schedule
  - "Chấm dứt hợp đồng" button (if lease is active)
  - "Đóng" button
- Terminate confirmation dialog:
  - Prompt for termination date (Input type="date")
  - Optional notes textarea
  - "Xác nhận chấm dứt" / "Hủy" buttons
  - Calls useTerminateLease mutation

Handlers:
- handleOpenCreate/Edit/Detail/CloseDialog for modal state
- handleSubmit: JSON body (convert dates to ISO strings) for create/edit
- handleDelete: confirm with "Bạn có chắc muốn xóa hợp đồng này?" dialog
- handleTerminate: opens termination dialog, calls terminate API

All Vietnamese text with proper diacritics. Button disabled states when no property exists.

**Sidebar.tsx update:**
- Add FileText icon import from lucide-react
- Add navigation item: { name: 'Hợp đồng', href: '/dashboard/leases', icon: FileText }
- Position AFTER "Thanh toán" and BEFORE "Điện nước"

**BottomNav.tsx update:**
- Add FileText icon import from lucide-react
- Add navigation item: { name: 'Hợp đồng', href: '/dashboard/leases', icon: FileText }
- Same position as Sidebar (after Thanh toán, before Điện nước)
  </action>
  <verify>
Run `npx tsc --noEmit` to verify TypeScript compilation. Verify page file exists at app/(dashboard)/dashboard/leases/page.tsx. Verify Sidebar.tsx contains 'Hợp đồng' and '/dashboard/leases'. Verify BottomNav.tsx contains same. Check that the page imports all necessary hooks: useLeases, useCreateLease, useUpdateLease, useDeleteLease, useTerminateLease, useLease.
  </verify>
  <done>
Leases page provides full CRUD with filtering, detail view with payment schedule table, and early termination dialog. Navigation updated in both sidebar and bottom nav with "Hợp đồng" link using FileText icon.
  </done>
</task>

</tasks>

<verification>
1. components/leases/ contains 5 component files
2. app/(dashboard)/dashboard/leases/page.tsx exists and renders lease management UI
3. Sidebar shows "Hợp đồng" navigation item with FileText icon after "Thanh toán"
4. BottomNav shows "Hợp đồng" navigation item
5. LeaseCard displays status badges (active=green, expiring_soon=amber, expired=gray, terminated=red)
6. LeaseForm has tenant selector, room selector, date pickers, rent/deposit inputs
7. LeaseScheduleTable shows monthly schedule with status indicators
8. Detail view shows lease info + schedule table
9. Terminate dialog prompts for date and notes
10. `npx tsc --noEmit` passes
</verification>

<success_criteria>
- LEASE-01: LeaseForm creates lease with start/end dates, monthly rent, deposit
- LEASE-02: LeaseForm links lease to tenant and room via Select dropdowns
- LEASE-03: After creation, detail view shows auto-generated payment schedule
- LEASE-04: Detail modal shows lease terms and payment schedule table
- LEASE-05: Edit mode allows changing dates, rent, deposit; schedule regenerates
- LEASE-06: LeaseCard shows lease renewal status with colored badges (active, expiring soon, expired)
- LEASE-07: Terminate dialog allows ending lease early with move-out date
- Navigation accessible from sidebar and bottom nav
</success_criteria>

<output>
After completion, create `.planning/phases/05-lease-management/05-03-SUMMARY.md`
</output>
