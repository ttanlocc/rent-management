---
phase: 04-payment-tracking
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - components/payments/PaymentCard.tsx
  - components/payments/PaymentList.tsx
  - components/payments/PaymentForm.tsx
  - components/payments/PaymentEmptyState.tsx
  - app/(dashboard)/dashboard/payments/page.tsx
  - components/layout/Sidebar.tsx
  - components/layout/BottomNav.tsx
autonomous: false

must_haves:
  truths:
    - "User can navigate to payments page from sidebar and bottom nav"
    - "User can see payment list with overdue payments highlighted in red"
    - "User can create a payment with date, amount, method, status, and optional receipt"
    - "User can edit payment details via the form"
    - "User can delete a payment with confirmation dialog"
    - "User can filter payments by status and date range"
    - "User can upload receipt image in the payment form"
    - "Overdue payments show red badge and red border styling"
  artifacts:
    - path: "components/payments/PaymentCard.tsx"
      provides: "Individual payment display with overdue highlighting"
      min_lines: 40
    - path: "components/payments/PaymentList.tsx"
      provides: "Payment list with loading skeletons and empty state"
      min_lines: 30
    - path: "components/payments/PaymentForm.tsx"
      provides: "Create/edit payment form with receipt upload"
      min_lines: 80
    - path: "components/payments/PaymentEmptyState.tsx"
      provides: "Empty state when no payments exist"
      min_lines: 15
    - path: "app/(dashboard)/dashboard/payments/page.tsx"
      provides: "Payments page with full CRUD and filtering"
      min_lines: 80
    - path: "components/layout/Sidebar.tsx"
      provides: "Updated sidebar with payments nav link"
      contains: "payments"
    - path: "components/layout/BottomNav.tsx"
      provides: "Updated bottom nav with payments link"
      contains: "payments"
  key_links:
    - from: "app/(dashboard)/dashboard/payments/page.tsx"
      to: "hooks/usePayments.ts"
      via: "import CRUD hooks"
      pattern: "usePayments|useCreatePayment|useUpdatePayment|useDeletePayment"
    - from: "components/payments/PaymentForm.tsx"
      to: "lib/validations/payment.ts"
      via: "import form schema and enums"
      pattern: "paymentFormSchema|PAYMENT_METHODS|PAYMENT_STATUSES"
    - from: "components/payments/PaymentForm.tsx"
      to: "hooks/usePayments.ts"
      via: "import buildPaymentFormData helper"
      pattern: "buildPaymentFormData"
    - from: "components/payments/PaymentCard.tsx"
      to: "lib/utils/formatters.ts"
      via: "import formatCurrency and formatDate"
      pattern: "formatCurrency|formatDate"
    - from: "components/layout/Sidebar.tsx"
      to: "app/(dashboard)/dashboard/payments/page.tsx"
      via: "navigation link"
      pattern: "/dashboard/payments"
---

<objective>
Build the complete payment tracking UI: payment components (card, list, form with receipt upload, empty state), the payments page with CRUD and filtering, and navigation updates.

Purpose: Deliver the user-facing payment tracking experience. Landlords can create, view, edit, delete payments, upload receipts, filter by status/date, and see overdue indicators.
Output: Payment UI components, payments page, updated navigation.
</objective>

<execution_context>
@C:/Users/LocTran/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LocTran/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-payment-tracking/04-RESEARCH.md
@.planning/phases/04-payment-tracking/04-01-SUMMARY.md
@.planning/phases/04-payment-tracking/04-02-SUMMARY.md

@components/tenants/TenantCard.tsx
@components/tenants/TenantList.tsx
@components/tenants/TenantForm.tsx
@components/tenants/TenantEmptyState.tsx
@app/(dashboard)/dashboard/tenants/page.tsx
@components/layout/Sidebar.tsx
@components/layout/BottomNav.tsx
@hooks/usePayments.ts
@lib/validations/payment.ts
@lib/utils/formatters.ts
@components/ui/badge.tsx
@components/ui/card.tsx
@components/ui/form.tsx
@components/ui/select.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create payment UI components</name>
  <files>components/payments/PaymentCard.tsx, components/payments/PaymentList.tsx, components/payments/PaymentForm.tsx, components/payments/PaymentEmptyState.tsx</files>
  <action>
Create all four payment components following existing tenant component patterns. All files need `'use client'` directive.

**PaymentEmptyState.tsx** (follow TenantEmptyState pattern):
- Props: `onAddPayment: () => void`
- Display centered empty state with Receipt icon (lucide-react), heading "Chua co thanh toan nao" (with diacritics), description text, and "Them thanh toan" button
- Use same styling as TenantEmptyState

**PaymentCard.tsx** (follow TenantCard pattern, with overdue logic from research):
- Props: `payment: Payment` (import type from usePayments), `onEdit: (payment) => void`, `onDelete: (payment) => void`
- Overdue detection: `const isOverdue = payment.payment_status !== 'paid_in_full' && new Date(payment.payment_date) < new Date()`
- Card styling: If overdue, apply `border-red-200 bg-red-50` classes (red highlight per PAY-07)
- Content layout:
  - Top row: Amount (formatCurrency) + status badge + overdue badge (if applicable)
  - Overdue badge: `<Badge variant="destructive" className="gap-1"><AlertCircle className="h-3 w-3" />Qua han</Badge>` (with diacritics)
  - Status badge: Map payment_status to labels/variants:
    - paid_in_full -> "Da thanh toan" / variant="default"
    - partial_payment -> "Thanh toan 1 phan" / variant="secondary"
    - late_fee_applied -> "Phat tre han" / variant="outline"
  - Second row: Tenant name (payment.tenant?.full_name) + formatted date
  - Third row: Payment method label + receipt indicator (Receipt icon + "Co bien lai" if receipt_url exists)
  - Notes: Show notes text if present (truncated to ~60 chars)
- Action buttons: Edit (Edit icon) and Delete (Trash2 icon) using ghost Button variant
- Use formatCurrency and formatDate from `@/lib/utils/formatters`
- Use Badge, Card, Button from shadcn/ui components
- Icons: AlertCircle, Receipt, Edit, Trash2 from lucide-react
- Use cn() from @/lib/utils for conditional classes

**PaymentList.tsx** (follow TenantList pattern):
- Props: `payments: Payment[]`, `isLoading: boolean`, `onAddPayment: () => void`, `onEditPayment: (payment) => void`, `onDeletePayment: (payment) => void`
- Loading state: Grid of 4 skeleton cards (Skeleton component, similar to TenantList)
- Empty state: Render `<PaymentEmptyState onAddPayment={onAddPayment} />`
- List: `<div className="space-y-3">` with PaymentCard for each payment (use space-y-3 list layout, not grid, since payment cards are wider with more info)

**PaymentForm.tsx** (follow TenantForm pattern, extended with file upload):
- Props: `payment?: any` (for edit mode), `tenants: Array<{id, full_name, room?: {name}}>` (for tenant selection dropdown), `onSubmit: (formData: FormData) => void`, `onCancel?: () => void`, `isSubmitting?: boolean`
- Use react-hook-form with zodResolver and paymentFormSchema
- Import PAYMENT_METHODS and PAYMENT_STATUSES from validations for Select dropdowns
- Import buildPaymentFormData from hooks/usePayments

- Form header: Receipt icon + "Them thanh toan" / "Sua thanh toan" heading (with diacritics, like TenantForm pattern)

- Fields layout (grid gap-4 sm:grid-cols-2):
  1. **Tenant selection** (sm:col-span-2): Select dropdown with tenant list. Each option shows "full_name - room.name". If editing, default to payment.tenant_id.
  2. **Payment date**: Input type="date" with Calendar icon, same pattern as TenantForm move_in_date
  3. **Amount**: Input type="number" with DollarSign icon (or use Banknote from lucide), step="1000", placeholder "0"
  4. **Payment method**: Select dropdown using PAYMENT_METHODS array
  5. **Payment status**: Select dropdown using PAYMENT_STATUSES array
  6. **Transaction reference**: Input text with Hash icon, placeholder "So giao dich / ma chuyen khoan"
  7. **Receipt upload** (sm:col-span-2):
     - Native file input (`<input type="file" accept="image/jpeg,image/png,image/webp,application/pdf" />`)
     - Register with form as receipt_file field
     - Show file name after selection
     - If editing and payment.receipt_url exists, show "Bien lai hien tai" link (opens in new tab) with option to replace
     - Max 5MB indicator text
  8. **Notes** (sm:col-span-2): Textarea (use Input or a simple textarea) for notes, placeholder "Ghi chu them..."

- Submit handler:
  ```typescript
  const handleSubmit = (data: PaymentFormInput) => {
    const { receipt_file, ...paymentData } = data
    const formData = buildPaymentFormData(
      {
        ...paymentData,
        payment_date: data.payment_date, // Date object -> handled by buildPaymentFormData
      },
      receipt_file
    )
    onSubmit(formData)
  }
  ```
  For edit mode, construct update data similarly but use PUT-compatible format (the page handler will handle the mutation call).

- Footer: Cancel + Submit buttons (same pattern as TenantForm)

Use all proper Vietnamese diacritics in labels and placeholder text (matching the patterns in TenantForm.tsx).
  </action>
  <verify>
Run `npx tsc --noEmit --pretty 2>&1 | head -40` to verify no TypeScript errors. Verify all 4 component files exist in components/payments/.
  </verify>
  <done>
Four payment components created: PaymentCard with overdue red highlighting (PAY-07), PaymentList with loading/empty states, PaymentForm with all fields including receipt upload (PAY-01 through PAY-08), PaymentEmptyState. All follow existing component patterns.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create payments page and update navigation</name>
  <files>app/(dashboard)/dashboard/payments/page.tsx, components/layout/Sidebar.tsx, components/layout/BottomNav.tsx</files>
  <action>
Create `app/(dashboard)/dashboard/payments/page.tsx` following `app/(dashboard)/dashboard/tenants/page.tsx` exactly:

1. **'use client'** directive
2. **Imports**: useState, payment hooks (usePayments, useCreatePayment, useUpdatePayment, useDeletePayment) from `@/hooks/usePayments`, useProperties and useTenants for property context and tenant dropdown, PaymentList/PaymentForm components, Button, Input, Card, CardContent, Select/SelectContent/SelectItem/SelectTrigger/SelectValue, Plus, Search, Filter, AlertTriangle icons

3. **State**:
   - `dialogMode: 'closed' | 'create' | 'edit'`
   - `selectedPayment: any | null`
   - `statusFilter: string` ('' for all, or specific status)
   - `tenantFilter: string` ('' for all, or specific tenant_id)
   - `overdueOnly: boolean` (false by default)

4. **Data fetching**:
   - `useProperties()` for property context (same pattern as tenants page)
   - `useTenants({})` to get tenant list for form dropdown and filter dropdown
   - `usePayments({ payment_status: statusFilter || undefined, tenant_id: tenantFilter || undefined, overdue_only: overdueOnly ? 'true' : undefined })`

5. **Mutations**: useCreatePayment, useUpdatePayment, useDeletePayment

6. **Handlers** (same pattern as tenants page):
   - `handleOpenCreate()`: set dialogMode to 'create', clear selectedPayment
   - `handleOpenEdit(payment)`: set selectedPayment, set dialogMode to 'edit'
   - `handleCloseDialog()`: set dialogMode to 'closed', clear selectedPayment
   - `handleSubmit(formData: FormData)`:
     - For create: `createPayment.mutate(formData, { onSuccess: handleCloseDialog })`
     - For edit: Extract JSON data from formData, call `updatePayment.mutate({ id: selectedPayment.id, data: parsedData }, { onSuccess: handleCloseDialog })`
   - `handleDelete(payment)`: `window.confirm('Ban co chac muon xoa thanh toan nay?')` then `deletePayment.mutate(payment.id)` (with diacritics)

7. **Page layout**:
   - **Header row**: "Quan ly thanh toan" heading (h1, 2xl bold) + subtitle with count + "Them thanh toan" button (Plus icon)
   - **No property warning**: Same pattern as tenants page if no properties exist
   - **Filter bar** (below header, horizontal flex with gap):
     - Status filter: Select dropdown with "Tat ca trang thai" placeholder + PAYMENT_STATUSES options + "Qua han" option that sets overdueOnly
     - Tenant filter: Select dropdown with "Tat ca khach thue" placeholder + tenant list options
     - OR simpler approach: Two Select filters side by side + a toggle/checkbox for "Chi hien qua han"
   - **Payment list**: `<PaymentList payments={payments} isLoading={isLoading} onAddPayment={handleOpenCreate} onEditPayment={handleOpenEdit} onDeletePayment={handleDelete} />`
   - **Create/Edit Modal**: Same Card modal overlay pattern as tenants page. Pass tenants array to PaymentForm for the tenant dropdown.

Update `components/layout/Sidebar.tsx`:
- Add payment nav item to the `navigation` array, positioned AFTER "Nguoi thue" and BEFORE "Dien nuoc":
  ```typescript
  { name: 'Thanh toan', href: '/dashboard/payments', icon: Banknote },
  ```
  Import `Banknote` from lucide-react (appropriate icon for payments). If Banknote is not available, use `Wallet` or `CreditCard`.

Update `components/layout/BottomNav.tsx`:
- Add payment nav item to the `navigation` array, same position (after Nguoi thue, before Dien nuoc):
  ```typescript
  { name: 'Thanh toan', href: '/dashboard/payments', icon: Banknote },
  ```
  Import `Banknote` from lucide-react.

Use proper Vietnamese diacritics throughout (matching existing patterns in the codebase).
  </action>
  <verify>
Run `npx tsc --noEmit --pretty 2>&1 | head -40` to verify no TypeScript errors. Run `npm run build 2>&1 | tail -20` to verify the page builds. Navigate to http://localhost:3000/dashboard/payments and verify the page renders (dev server must be running).
  </verify>
  <done>
Payments page renders with header, filter bar (status + tenant filters), payment list with overdue highlighting, create/edit modal with receipt upload. Sidebar and bottom nav include "Thanh toan" link. All PAY-01 through PAY-08 requirements addressed in UI.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete payment tracking feature</name>
  <files>app/(dashboard)/dashboard/payments/page.tsx</files>
  <action>
Human verification of the complete payment tracking feature built across all 3 plans (data foundation, API/hooks, UI).

Verify the following user flows work end-to-end:
1. Navigate to payments page via sidebar/bottom nav
2. Create a payment with all fields
3. Create an overdue payment and verify red indicators
4. Upload a receipt
5. Edit and delete payments
6. Use filters
  </action>
  <verify>
1. Navigate to http://localhost:3000/dashboard/payments
2. Verify "Thanh toan" appears in sidebar and bottom navigation
3. Click "Them thanh toan" - verify form opens with all fields (tenant, date, amount, method, status, reference, receipt upload, notes)
4. Create a payment record - verify it appears in the list
5. Create a payment with status "partial_payment" and a past date - verify it shows red overdue badge and red border
6. Upload a receipt image when creating a payment - verify it is accepted
7. Click edit on a payment - verify form pre-fills with existing data
8. Update a payment - verify changes are reflected
9. Delete a payment - verify confirmation dialog appears and payment is removed
10. Test status filter - verify list filters correctly
11. Test tenant filter - verify list shows only selected tenant's payments
  </verify>
  <done>
All PAY-01 through PAY-08 requirements verified working in the UI. Payment CRUD, receipt upload, filtering, and overdue indicators all functional.
  </done>
</task>

</tasks>

<verification>
- Payments page accessible at /dashboard/payments
- Navigation updated in both sidebar and bottom nav
- All CRUD operations work (create, read, update, delete)
- Overdue payments show red indicators (PAY-07)
- Receipt upload works in create form (PAY-03)
- Filters work for status and tenant (PAY-04)
- Delete has confirmation dialog (PAY-06)
- Notes field available (PAY-08)
- TypeScript compiles: `npx tsc --noEmit`
- Build succeeds: `npm run build`
</verification>

<success_criteria>
- PAY-01: Payment with date, amount, method recorded successfully
- PAY-02: Payment status (paid, partial, late fee) selectable and displayed
- PAY-03: Receipt upload works in payment form
- PAY-04: Payment history viewable per tenant with filtering
- PAY-05: Payment editable (amount, date, status, notes)
- PAY-06: Payment deletable with confirmation
- PAY-07: Overdue payments have red badge and red border
- PAY-08: Notes/memo field available and saved
</success_criteria>

<output>
After completion, create `.planning/phases/04-payment-tracking/04-03-SUMMARY.md`
</output>
